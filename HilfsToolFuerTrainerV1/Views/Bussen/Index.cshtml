@model IEnumerable<HilfsToolFuerTrainerV1.Models.T_Bussen>

@{
    ViewBag.Title = "Index";
}

<h2>Bussen Liste</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grund)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Betrag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.istBezahlt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.T_Spieler.Vorname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grund)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Betrag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.istBezahlt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.T_Spieler.Vorname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
